%div.chat-side
  .side-header
    .side-header__box
      %p.side-header__box__user-name 
        = current_user.name
      .side-header__box__right-box
        = link_to new_group_path, class: "side-header__box__right-box__make-group" do
          = icon('fas', 'edit', class:'icon-edit')
        = link_to edit_user_path(current_user) , class: "side-header__box__right-box__edit-user" do
          = icon('fas', 'cog', class:'icon-cog')
  .groups
    - current_user.groups.each do |group|
      .group
        =  link_to '#'  do
          %p.group__name
            =  group.name
          %p.group__lastest-message 
            まだメッセージはありません 